import XCTest
@testable import ODDependencyInjection

final class ODDependencyInjectionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ODDependencyInjection().text, "Hello, World!")
    }
}
